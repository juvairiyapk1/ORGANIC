package com.timeco.application.Service.payment;

import com.razorpay.RazorpayException;
import com.timeco.application.model.order.PurchaseOrder;
import com.timeco.application.model.user.Address;
import com.timeco.application.model.user.User;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import java.util.Map;

@Service
public interface PaymentService {
//    void handleRazorpayPayment(Map<String, Object> response, double totalAmount, PurchaseOrder purchaseOrder, String username, Address address) throws RazorpayException;
//
//    void handleWalletPayment(Map<String, Object> response, double totalAmount, User user, PurchaseOrder purchaseOrder);

//    String createOrder(int amount, String currency, String receipt) throws RazorpayException;
}

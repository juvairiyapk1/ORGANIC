
## spring dataSource configuration
spring.datasource.url= jdbc:mysql://localhost:3306/ORGANIC?useSSL=false
spring.datasource.username=root
spring.datasource.password=password12345



## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#Used to control whether or not the SQL statements generated by JPA are formatted
spring.jpa.properties.hibernate.format_sql=true

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#debug logs for App security
logging.level.org.springframework.security=DEBUG


#debug logs for SQL JPA
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

spring.main.allow-circular-references=true

# Set the root logger's logging level to INFO
logging.level.root=INFO


#mailsender
# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=juvairiyabinthkoya@gmail.com
spring.mail.password=iwct rgdx xioo oprn
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true

spring.servlet.multipart.enabled=true
# Maximum file size allowed (in bytes)
spring.servlet.multipart.max-file-size=10MB
# Maximum request size allowed (in bytes)
spring.servlet.multipart.max-request-size=10MB

spring.session.store-type=jdbc